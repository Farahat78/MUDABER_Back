# Environment Variables — Prediction Pipeline
# Copy to .env and fill in your values
# NEVER commit .env to Git.

# (Optional) GitHub token for push (alternative to SSH key)
# GITHUB_TOKEN=your_token_here
